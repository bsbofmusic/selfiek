{
  "name": "selfiek-monorepo",
  "private": true,
  "version": "3.8.1",
  "workspaces": [
    "packages/selfiek"
  ],
  "scripts": {
    "build": "npm run build --workspace selfiek",
    "test": "cargo test --workspace && npm test --workspace selfiek",
    "pack:dry": "npm pack --workspace selfiek --dry-run"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "typescript": "^5.9.3"
  }
}
